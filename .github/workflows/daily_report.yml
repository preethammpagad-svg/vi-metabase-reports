name: VI Daily Reports

# 04:00 IST = 22:30 UTC
on:
  schedule:
    - cron: "30 22 * * *"     # runs daily at 22:30 UTC → 04:00 IST
  workflow_dispatch: {}

concurrency:
  group: vi-daily-reports
  cancel-in-progress: true

jobs:
  run-report:
    runs-on: ubuntu-latest

    env:
      METABASE_SITE: ${{ secrets.METABASE_SITE }}
      METABASE_API_KEY: ${{ secrets.METABASE_API_KEY }}

      SMTP_HOST: ${{ secrets.SMTP_HOST }}
      SMTP_PORT: ${{ secrets.SMTP_PORT }}
      SMTP_USER: ${{ secrets.SMTP_USER }}
      SMTP_PASS: ${{ secrets.SMTP_PASS }}

      # Email setup
      FROM_EMAIL: ${{ secrets.FROM_EMAIL }}      # ritik.batham@skit.ai
      TO_EMAIL: ${{ secrets.TO_EMAIL }}          # comma-separated list
      CC_EMAIL: ${{ secrets.CC_EMAIL }}          # monalisa

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.13"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Run daily Metabase export
        run: python -u metabase_daily_export_and_email.py
